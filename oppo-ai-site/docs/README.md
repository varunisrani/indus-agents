Oppo AI static site

To run locally: open the HTML files in a browser. This is a static site; no server required.

Deployment: Deploy to GitHub Pages or any static host. Ensure assets are uploaded.

Form: Contact form posts to Formspree by default. Replace action in contact.html with your form endpoint.

Accessibility: Target WCAG 2.1 AA. Run axe or Lighthouse during QA.